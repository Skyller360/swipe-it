import colorReducer from './reducers';

export { default as colorSelectors } from './selectors';
export { default as colorOperations } from './operations';
export { default as colorTypes } from './types';

export default colorReducer;
